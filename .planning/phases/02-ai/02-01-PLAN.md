---
phase: 02-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autoload/game_manager.gd
  - scripts/events/ai_thinking_event.gd
  - scripts/panels/hud.gd
  - scenes/panels/hud.tscn
  - translations/zh_CN.json
  - translations/en_US.json
  - test/test_game_manager.gd
autonomous: true
requirements: [AI-01, AI-02, AI-03, AI-04]
user_setup: []

must_haves:
  truths:
    - "Player can select Easy/Medium/Hard difficulty and observe distinctly different AI play quality"
    - "AI uses Minimax with alpha-beta pruning for optimal move selection"
    - "AI show thinking delays appropriate to difficulty level (Easy 0.5-1.0s, Medium 0.3-0.6s, Hard 0.2-0.4s)"
    - "HUD displays 'AI thinking...' / 'AI 思考中...' during AI's turn"
    - "AI difficulty selection persists across sessions via ConfigFile"
    - "Hard difficulty AI is undefeatable (perfect play)"
  artifacts:
    - path: "autoload/game_manager.gd"
      provides: "AI engine with alpha-beta pruning, difficulty parameterization, thinking delays, AiThinkingEvent publishing"
      contains: "_minimax(is_maximizing, depth, alpha, beta, max_depth, random_chance)"
    - path: "scripts/events/ai_thinking_event.gd"
      provides: "AiThinkingEvent class with thinking: bool field"
      contains: "class_name AiThinkingEvent"
    - path: "scripts/panels/hud.gd"
      provides: "HUD thinking indicator subscriber and handler"
      contains: "_on_ai_thinking"
    - path: "scenes/panels/hud.tscn"
      provides: "ThinkingLabel node in HUD scene"
      contains: "ThinkingLabel"
    - path: "translations/zh_CN.json"
      provides: "Chinese AI thinking label translation"
      contains: "ai_thinking"
    - path: "translations/en_US.json"
      provides: "English AI thinking label translation"
      contains: "ai_thinking"
    - path: "test/test_game_manager.gd"
      provides: "Updated and new AI tests for parameterized minimax"
      contains: "test_ai_hard_perfect"
  key_links:
    - from: "_schedule_ai_move"
      to: "AiThinkingEvent"
      via: "EventBus.publish(AiThinkingEvent.new(true))"
      pattern: "AiThinkingEvent\\.new\\(true\\)"
    - from: "_do_ai_move"
      to: "AiThinkingEvent"
      via: "EventBus.publish(AiThinkingEvent.new(false))"
      pattern: "AiThinkingEvent\\.new\\(false\\)"
    - from: "_find_best_move"
      to: "SettingsManager"
      via: "SettingsManager.get_value('gameplay/difficulty')"
      pattern: "SettingsManager\\.get_value"
    - from: "_on_ai_thinking"
      to: "thinking_label"
      via: "visibility toggle"
      pattern: "thinking_label\\.visible"
---

<objective>
Upgrade the basic AI to a polished three-tier system with Minimax + alpha-beta pruning.

Purpose: Deliver the final phase of the game — a smart AI opponent with clearly differentiated difficulty tiers that players can progressively challenge. The Easy tier lets casual players win comfortably, Medium provides a fair challenge, and Hard (perfect play) is the aspirational target: "Can you force a draw against a perfect opponent?"

Output: Parameterized alpha-beta minimax serving all three difficulties, randomized thinking delays per tier, HUD thinking indicator with bilingual support, and full test coverage for the new AI engine.
</objective>

<execution_context>
@E:/godot_learning/projects/cookery/.claude/gsd-core/workflows/execute-plan.md
@E:/godot_learning/projects/cookery/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai/02-CONTEXT.md
@.planning/phases/02-ai/02-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
</context>

<user_story_note>
NOTE: The ROADMAP.md goal line for Phase 2 ("Smart opponent with three-tier difficulty stands as the polished final challenge") is not in user-story format. Per MVP mode rules, this should ideally be formatted via `/gsd mvp-phase 02`. However, since this phase has been fully discussed with all decisions locked in CONTEXT.md, I formulate the following user story from context for the plan:

**As a** player, **I want to** face a smart opponent with three-tier difficulty and visible thinking feedback, **so that** I can choose my challenge level from casual play (Easy) to forcing a draw against a perfect opponent (Hard).

If this formulation is incorrect, run `/gsd mvp-phase 02` to set the official user story.
</user_story_note>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write/update tests for parameterized AI API</name>
  <files>test/test_game_manager.gd</files>
  <read_first>
    - test/test_game_manager.gd — existing test patterns, preload convention, assertion API
    - autoload/game_manager.gd — existing AI function signatures to understand the API being replaced
  </read_first>
  <behavior>
    - Test 1: test_ai_first_move_center passes before any board state — _find_best_move returns 4 with any difficulty parameters (per D-03, first move is always center regardless of difficulty)
    - Test 2: test_ai_blocks_win — AI (O) blocks X's row win at cell 2 with max_depth=9, random_chance=0.0
    - Test 3: test_ai_takes_win — AI (O) takes immediate win at cell 2 with max_depth=9, random_chance=0.0
    - Test 4: test_ai_hard_perfect — Hard AI (O) never loses from a position where X has one corner and center is empty; runs multiple starting positions
    - Test 5: test_ai_easy_random — Easy AI (depth=1, random=0.8) does not always pick the optimal move over 20 trials
    - Test 6: test_ai_medium_blocks — Medium AI (depth=4, random=0.15) blocks obvious two-in-a-row threats
  </behavior>
  <action>
    Update `test/test_game_manager.gd`:

    a. Replace existing AI tests (`test_ai_first_move_center`, `test_ai_blocks_win`, `test_ai_takes_win`) that call `_find_best_move()` without arguments.

    b. Each AI test now passes explicit arguments: `_find_best_move(max_depth, random_chance)` where:
       - Hard tests use `max_depth=9, random_chance=0.0`
       - Medium tests use `max_depth=4, random_chance=0.15`
       - Easy tests use `max_depth=1, random_chance=0.8`
       - `test_ai_first_move_center` uses `max_depth=9, random_chance=0.0`

    c. Add constant `const INF_SCORE := 1000000` at class scope (not inside test methods) for use in test assertions that need to reference score boundaries.

    d. Add `test_ai_hard_perfect`:
       - Create a GameManager instance via `autoqfree(GameManagerClass.new())`
       - Set `gm.ai_player = gm.Player.O`, `gm.current_player = gm.Player.O`
       - Test multiple board states where AI (O) should not lose:
         - X at center (4), O at corner (0), X at opposite corner (8) → O should block at edge (3 or 5 or 1 or 7)
         - X at corner (0), O at center (4), X at another corner (2) → O should block at edge
       - For each state: call `_find_best_move(9, 0.0)`, place the AI move, then simulate all possible X responses; verify no X response leads to X winning
       - Mark this test as `# SLOW: runs full tree search` so the executor knows expected runtime

    e. Add `test_ai_easy_random`:
       - Set up board: `gm.board = [0, -1, -1, 1, -1, -1, -1, -1, -1]`, `gm.move_count = 2`
       - Run `_find_best_move(1, 0.8)` 20 times
       - Assert that at least 3 out of 20 trials picked a suboptimal move (not blocking X's row win at cell 2)
       - Statistical test, not deterministic

    f. Add `test_ai_medium_blocks`:
       - Set up board where X has two in a row: `gm.board = [0, 0, -1, -1, 1, -1, -1, -1, -1]`, `gm.move_count = 3`, `gm.ai_player = gm.Player.O`, `gm.current_player = gm.Player.O`
       - Call `_find_best_move(4, 0.15)`
       - Since random_chance=0.15 is low, most runs should block at cell 2 — assert result is 2 (or fallback to statistical assertion if needed)

    g. Update the class-level `const` pattern: all AI tests use `autoqfree(GameManagerClass.new())` and set `gm.ai_player` explicitly since AI tests need to know which player the AI controls.

    Tests will fail initially because `_find_best_move()` and `_minimax()` still use old signatures — this is the intended RED state.
  </action>
  <verify>
    <automated>cd E:/godot_learning/projects/fast_prototype_test && godot --headless --addon gut/autoload/gut.gd --path . -bg --exit | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - test/test_game_manager.gd contains no calls to `_find_best_move()` with zero arguments (all calls pass `max_depth` and `random_chance` per D-03)
    - test/test_game_manager.gd contains `test_ai_hard_perfect` function
    - test/test_game_manager.gd contains `test_ai_easy_random` function
    - test/test_game_manager.gd contains `test_ai_medium_blocks` function
    - Running GUT tests prints errors about missing function signatures (expected RED state)
  </acceptance_criteria>
  <done>
    - test/test_game_manager.gd has 0 calls to `_find_best_move()` with zero arguments — all AI tests pass explicit max_depth and random_chance arguments
    - test/test_game_manager.gd defines test_ai_hard_perfect, test_ai_easy_random, test_ai_medium_blocks as test functions
    - GUT test run shows failures due to missing new _minimax/_find_best_move signatures (RED state) — not syntax errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement core AI rewrite — alpha-beta minimax, difficulty parameterization, thinking delay, AiThinkingEvent</name>
  <files>
    autoload/game_manager.gd
    scripts/events/ai_thinking_event.gd
  </files>
  <read_first>
    - autoload/game_manager.gd — current AI functions, timer pattern, event publishing pattern, GameState enum
    - scripts/events/cell_placed_event.gd — event class_name pattern (extends Event, class_name, p_-prefixed _init, typed fields) per D-07
    - .planning/phases/02-ai/02-RESEARCH.md — full code examples for _minimax, _find_best_move, _evaluate, _schedule_ai_move, _do_ai_move; pitfall warnings for double-move, nested break, stale timer, HUD race conditions
    - autoload/settings_manager.gd — get_value("gameplay/difficulty") API
  </read_first>
  <action>
    Create `scripts/events/ai_thinking_event.gd`:

    a. Extends `Event`, declare `class_name AiThinkingEvent` (per D-07 convention matching cell_placed_event.gd)
    b. Field: `var thinking: bool`
    c. Constructor: `func _init(p_thinking: bool) -> void:` sets `thinking = p_thinking`

    Modify `autoload/game_manager.gd`:

    a. Add constant `const INF_SCORE := 1000000` at the class level (per D-03, used by _minimax for alpha-beta initial values).

    b. Add difficulty constants (per D-02, D-03 matching SettingsManager convention 0=Easy, 1=Medium, 2=Hard):
       const EASY := 0
       const MEDIUM := 1
       const HARD := 2

    c. Replace `_schedule_ai_move()` (line 167-168):
       - Publish `AiThinkingEvent.new(true)` first via EventBus (per D-05, D-07)
       - Read difficulty via `SettingsManager.get_value("gameplay/difficulty", 1) as int` (default 1 = Medium)
       - Calculate random delay via `_get_ai_delay(difficulty)` (new function)
       - Defer timer start via `call_deferred("_start_ai_timer", delay)` (per RESEARCH.md pitfall 4 — prevents HUD race condition on Hard difficulty where 0.2s delay may hide the thinking indicator)

    d. Add function `_get_ai_delay(difficulty: int) -> float`:
       - match difficulty: 0: return randf_range(0.5, 1.0), 1: return randf_range(0.3, 0.6), 2: return randf_range(0.2, 0.4), default: return 0.5 (per D-04 uniform distribution within range)

    e. Add function `_start_ai_timer(delay: float) -> void`:
       - Guard: if state != GameState.PLAYING: publish AiThinkingEvent.new(false) and return (per pitfall 3 — game may have ended between thinking event and timer start)
       - Call ai_timer.start(delay)

    f. Replace `_do_ai_move()` (line 171-174):
       - Guard: if state != GameState.PLAYING: publish AiThinkingEvent.new(false) and return (per pitfall 3 — stale timer guard)
       - Read difficulty from SettingsManager
       - Map difficulty to max_depth and random_chance:
         - Easy (0): max_depth=1, random_chance=0.8 (depth-1 catches immediate wins/blocks; 80% random = mostly random but not broken)
         - Medium (1): max_depth=4, random_chance=0.15 (depth 4 sees multi-move sequences; 15% random keeps it beatable)
         - Hard (2): max_depth=9, random_chance=0.0 (full search, perfect play per D-02)
       - Call var best := _find_best_move(max_depth, random_chance)
       - If best >= 0: call place_mark(best) (this publishes CellPlacedEvent internally)
       - Publish AiThinkingEvent.new(false) after place_mark (per D-07)

    g. Replace `_find_best_move()` (line 177-191) with new signature `func _find_best_move(max_depth: int, random_chance: float) -> int`:
       - If move_count == 0: return 4 (center on first move)
       - var best_score: int = -INF_SCORE
       - var best_index := -1
       - Loop for i in 9: if board[i] == -1: set board[i] = ai_player, call _minimax(false, 1, -INF_SCORE, INF_SCORE, max_depth, random_chance), restore board[i] = -1, track best
       - Critical: pass is_maximizing=false and depth=1 — AI's candidate is already placed, so opponent evaluates next (per RESEARCH.md pitfall 1 — double-move prevention)
       - Fallback: if best_index == -1, call _pick_random_empty()

    h. Add function `_pick_random_empty() -> int`:
       - Collect empty cell indices, return one at random via randi() % empties.size(), return -1 if none

    i. Replace `_minimax()` (line 194-217) with full alpha-beta signature: `func _minimax(is_maximizing: bool, depth: int, alpha: int, beta: int, max_depth: int, random_chance: float) -> int`:
       - Terminal checks (order matters — check winner first):
         - var winner := _check_winner() — if winner != -1: return INF_SCORE - depth if winner == ai_player else depth - INF_SCORE (bigger constant than 10 to avoid heuristic overshadowing per RESEARCH.md pitfall 5)
         - if _is_board_full(): return 0
         - if depth >= max_depth: return _evaluate() (depth-limited search truncation)
       - Random injection: if random_chance > 0.0 and randf() < random_chance: return 0 (skip search for this branch)
       - Maximizing (AI's turn): var best := -INF_SCORE, loop empty cells, set board[i] = ai_player, recurse with _minimax(false, depth+1, alpha, beta, ...), restore board[i] = -1, best = max(best, score), alpha = max(alpha, best), if beta <= alpha: return best (RETURN not break — per RESEARCH.md pitfall 2)
       - Minimizing (opponent's turn): var best := INF_SCORE, determine opponent via var opponent := Player.X if ai_player == Player.O else Player.O, same loop pattern with _minimax(true, depth+1, ...), best = min(best, score), beta = min(beta, best), if beta <= alpha: return best

    j. Add function `_evaluate() -> int`:
       - Heuristic board evaluation for depth-limited search (used when max_depth prevents full search)
       - For each line in WIN_LINES: count AI marks, opponent marks, and check if line is still open (no opponent blocker)
       - For open lines with AI marks: ai_score += ai_count * ai_count (quadratic weighting: 2-in-a-row scores 4, 1-in-a-row scores 1)
       - For open lines with opponent marks: opp_score += opp_count * opp_count (same weighting)
       - Return ai_score - opp_score (positive = AI advantage)

    k. Update `_on_rematch()` and `reset_board()`: in both functions, call ai_timer.stop() if the timer is running, to prevent stale timer fire after game state transitions (per RESEARCH.md pitfall 3).

    l. No changes to place_mark() or any other public API — the AI flow is internal to GameManager.

    m. Keep the existing ai_timer Timer node setup (line 28-31) unchanged (per D-06 — no migration to await).
  </action>
  <verify>
    <automated>cd E:/godot_learning/projects/fast_prototype_test && godot --headless --addon gut/autoload/gut.gd --path . -bg --exit | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - autoload/game_manager.gd contains `_minimax(is_maximizing, depth, alpha, beta, max_depth, random_chance)` with alpha-beta pruning and return-on-cutoff pattern
    - autoload/game_manager.gd contains `_find_best_move(max_depth, random_chance)` — calls `_minimax(false, 1, ...)` (not true, 0) per double-move prevention
    - autoload/game_manager.gd contains `_evaluate()` function
    - autoload/game_manager.gd contains `_get_ai_delay(difficulty)` with randf_range matching D-04 ranges
    - autoload/game_manager.gd contains `_pick_random_empty()` function
    - autoload/game_manager.gd contains `_start_ai_timer(delay)` with call_deferred pattern
    - autoload/game_manager.gd publishes AiThinkingEvent.new(true) BEFORE timer start and AiThinkingEvent.new(false) AFTER place_mark() (per D-07)
    - autoload/game_manager.gd reads difficulty via SettingsManager.get_value("gameplay/difficulty") in both _schedule_ai_move and _do_ai_move
    - autoload/game_manager.gd calls ai_timer.stop() in reset_board() and _on_rematch()
    - scripts/events/ai_thinking_event.gd exists with class_name AiThinkingEvent, thinking: bool field, and p_-prefixed _init param
    - GUT tests pass: test_ai_first_move_center, test_ai_blocks_win, test_ai_takes_win, test_ai_hard_perfect, test_ai_easy_random, test_ai_medium_blocks
  </acceptance_criteria>
  <done>
    - autoload/game_manager.gd does NOT contain old `_minimax(is_maximizing, depth)` or `_find_best_move()` (old zero-argument signatures removed per D-01)
    - autoload/game_manager.gd contains new `_minimax(is_maximizing, depth, alpha, beta, max_depth, random_chance)` with alpha-beta pruning
    - scripts/events/ai_thinking_event.gd exists with class_name AiThinkingEvent
    - All 22 GUT tests pass including the new test_ai_hard_perfect, test_ai_easy_random, test_ai_medium_blocks
    - grep for "return best" after "beta <= alpha" confirms return-on-cutoff pattern (not break)
  </done>
</task>

<task type="auto">
  <name>Task 3: HUD thinking indicator and translation keys</name>
  <files>
    scripts/panels/hud.gd
    scenes/panels/hud.tscn
    translations/zh_CN.json
    translations/en_US.json
  </files>
  <read_first>
    - scripts/panels/hud.gd — existing event subscription pattern, @onready declarations, _on_ handler naming, _on_destroy pattern for cleanup
    - scenes/panels/hud.tscn — existing scene tree structure (VBoxContainer > ModeLabel, HBoxContainer with LeftVBox/CenterVBox/RightVBox)
    - translations/zh_CN.json — existing translation key format, hud section
    - translations/en_US.json — existing translation key format, hud section
  </read_first>
  <action>
    Modify `scenes/panels/hud.tscn`:
    a. Add ThinkingLabel node inside the existing `VBoxContainer/HBoxContainer/CenterVBox` (below TurnLabel):
       [node name="ThinkingLabel" type="Label" parent="VBoxContainer/HBoxContainer/CenterVBox"]
       layout_mode = 2
       text = "hud.ai_thinking"
       horizontal_alignment = 1
       theme_override_font_sizes/font_size = 14
       modulate = Color(0.91, 0.72, 0.29, 1)
       visible = false
       - modulate = gold/amber color to visually distinguish from the white turn indicator
       - visible = false by default — only shown during AI thinking

    Modify `scripts/panels/hud.gd`:
    b. Add `@onready var thinking_label: Label = $VBoxContainer/HBoxContainer/CenterVBox/ThinkingLabel`
       — typed as Label, follows existing @onready pattern

    c. In _ready(): add `EventBus.subscribe(&"AiThinkingEvent", _on_ai_thinking)` — follows existing subscription pattern (per D-05)

    d. In _on_destroy(): add `EventBus.unsubscribe(&"AiThinkingEvent", _on_ai_thinking)` — follows existing cleanup pattern

    e. Add handler function _on_ai_thinking(event: Event) -> void:
       - var is_thinking: bool = event.get("thinking")
       - thinking_label.visible = is_thinking
       - No additional logic — pure view toggle (per architecture responsibility map: HUD is view-only)

    f. LanguageChangedEvent handling: the existing _on_language_changed handler calls _refresh_all(). The thinking label's text uses tr("hud.ai_thinking") which Godot auto-updates on locale change. No explicit refresh needed.

    Modify `translations/zh_CN.json`:
    g. Add to "hud" section: "ai_thinking": "AI 思考中..."

    Modify `translations/en_US.json`:
    h. Add to "hud" section: "ai_thinking": "AI thinking..."
  </action>
  <verify>
    <automated>cd E:/godot_learning/projects/fast_prototype_test && godot --headless --addon gut/autoload/gut.gd --path . -bg --exit | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - scenes/panels/hud.tscn contains ThinkingLabel node under CenterVBox with visible=false
    - scripts/panels/hud.gd declares @onready var thinking_label referencing the new node
    - scripts/panels/hud.gd subscribes to &"AiThinkingEvent" in _ready()
    - scripts/panels/hud.gd unsubscribes from &"AiThinkingEvent" in _on_destroy()
    - scripts/panels/hud.gd contains _on_ai_thinking handler that toggles thinking_label.visible
    - translations/zh_CN.json "hud" section contains "ai_thinking": "AI 思考中..."
    - translations/en_US.json "hud" section contains "ai_thinking": "AI thinking..."
    - All GUT tests continue to pass
  </acceptance_criteria>
  <done>
    - hud.tscn has ThinkingLabel node with text="hud.ai_thinking", visible=false, gold modulate
    - hud.gd has @onready var thinking_label, AiThinkingEvent subscribe in _ready() and unsubscribe in _on_destroy(), _on_ai_thinking handler
    - zh_CN.json contains "ai_thinking": "AI 思考中..." under "hud" section
    - en_US.json contains "ai_thinking": "AI thinking..." under "hud" section
    - All 22 GUT tests pass after the changes
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
### New classes
- **AiThinkingEvent** (scripts/events/ai_thinking_event.gd) — Event class with thinking: bool field. Extends Event (mc_game_framework), uses class_name AiThinkingEvent, follows Phase 0 event convention.

### New functions
- _evaluate() -> int (game_manager.gd) — Board position heuristic for depth-limited search. Scores lines by AI/opponent mark count with quadratic weighting.
- _get_ai_delay(difficulty: int) -> float (game_manager.gd) — Returns random delay within difficulty range via randf_range(). Easy 0.5-1.0, Medium 0.3-0.6, Hard 0.2-0.4.
- _pick_random_empty() -> int (game_manager.gd) — Returns random empty cell index for fallback.
- _start_ai_timer(delay: float) -> void (game_manager.gd) — Deferred timer start with state guard. Called via call_deferred from _schedule_ai_move.

### Rewritten functions (new signatures)
- _minimax(is_maximizing: bool, depth: int, alpha: int, beta: int, max_depth: int, random_chance: float) -> int — Full alpha-beta pruning implementation replacing old _minimax(is_maximizing, depth). Uses return-on-cutoff, random injection for Easy/Medium, depth-limited search via max_depth.
- _find_best_move(max_depth: int, random_chance: float) -> int — Parameterized best-move search replacing old zero-argument version. Calls _minimax(false, 1, ...) for double-move prevention.
- _schedule_ai_move() — Now publishes AiThinkingEvent(true), reads difficulty from SettingsManager, calls _get_ai_delay for random delay, uses call_deferred for timer start.
- _do_ai_move() — Now reads difficulty from SettingsManager, maps to parameters, calls _find_best_move(max_depth, random_chance), publishes AiThinkingEvent(false).

### New constants
- INF_SCORE := 1000000 (game_manager.gd) — Infinity constant for alpha-beta initial values.
- EASY := 0, MEDIUM := 1, HARD := 2 (game_manager.gd) — Difficulty constants matching SettingsManager values.

### Modified functions
- reset_board() — Added ai_timer.stop() call for stale timer guard.
- _on_rematch() — Added ai_timer.stop() call for stale timer guard in _on_rematch handler.
- hud.gd:_ready() — Added AiThinkingEvent subscription.
- hud.gd:_on_destroy() — Added AiThinkingEvent unsubscription.

### New UI node
- ThinkingLabel (hud.tscn) — Label node inside CenterVBox, gold/amber modulate, tr("hud.ai_thinking"), visible=false by default.

### New translation keys
- hud.ai_thinking — "AI thinking..." (en_US), "AI 思考中..." (zh_CN)

### New test functions
- test_ai_hard_perfect — Verifies Hard AI never loses from multiple starting positions.
- test_ai_easy_random — Statistical test: Easy AI does not always pick optimal move over 20 trials.
- test_ai_medium_blocks — Medium AI blocks obvious two-in-a-row threats.

### Updated test functions
- test_ai_first_move_center — Updated to call _find_best_move(9, 0.0).
- test_ai_blocks_win — Updated to call _find_best_move(9, 0.0).
- test_ai_takes_win — Updated to call _find_best_move(9, 0.0).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SettingsManager -> GameManager | Difficulty value read from persisted config crosses from storage layer into game logic |

No other trust boundaries — all AI computation is local, no network, no user input parsing, no file I/O beyond existing SettingsManager.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | SettingsManager -> GameManager difficulty read | accept | SettingsManager stores difficulty from the Settings panel which only allows values 0/1/2 via UI. If the config file is manually edited, invalid difficulty falls through to the _ match arm in _do_ai_move() which defaults to Hard (max_depth=9, random_chance=0.0). No exploit vector. |
| T-02-02 | Tampering | ai_timer stale fire | mitigate | _do_ai_move() guards with if state != GameState.PLAYING: return and publishes AiThinkingEvent.new(false). reset_board() and _on_rematch() call ai_timer.stop(). Double protection covers both timer-before-end and end-during-deferred-timer scenarios. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. All changes are GDScript-only, no new external dependencies. |
</threat_model>

<verification>
1. All GUT tests pass: test_board_init, test_win_row, test_win_col, test_win_diag, test_win_anti_diag, test_no_winner_empty, test_no_winner_partial, test_board_full, test_board_not_full, test_ai_first_move_center, test_ai_blocks_win, test_ai_takes_win, test_ai_hard_perfect, test_ai_easy_random, test_ai_medium_blocks, test_get_cell, test_switch_turn, test_cursor_default, test_cursor_move_right, test_cursor_move_down, test_cursor_at_edge, test_cursor_move_while_game_over (22 total)

2. Verify all 4 requirement IDs are addressed:
   - AI-01: alpha-beta pruning in _minimax() + double-move prevention + return-on-cutoff
   - AI-02: three tiers via max_depth/random_chance parameters + SettingsManager integration
   - AI-03: _get_ai_delay() with randf_range per D-04 + AiThinkingEvent + HUD indicator
   - AI-04: Already implemented (Phase 0/1) — SettingsManager stores gameplay/difficulty; no new work

3. Verify all locked decisions D-01 through D-07 are implemented per CONTEXT.md
</verification>

<success_criteria>
1. Complete AI engine: _minimax with alpha-beta pruning serves all three difficulties via max_depth/random_chance parameters (AI-01, AI-02)
2. Thinking delays: Easy 0.5-1.0s, Medium 0.3-0.6s, Hard 0.2-0.4s randomized per move using randf_range (AI-03)
3. HUD indicator shows "AI thinking..." / "AI 思考中..." during AI turn via AiThinkingEvent subscription (AI-03)
4. Difficulty persisted via SettingsManager (no new persistence code needed -- AI-04 existing)
5. No stale timer fires after game state transitions (guards in _do_ai_move + timer stop in reset_board/_on_rematch)
6. HUD thinking indicator never flashes too briefly to see (call_deferred timer start guarantees at least one visible frame)
7. All GUT tests pass including new tests for hard perfect play, easy random behavior, and medium blocking
8. Old _minimax() and _find_best_move() signatures completely removed (D-01 -- no backward compatibility)
</success_criteria>

<output>
Create `.planning/phases/02-ai/02-01-SUMMARY.md` when done
</output>
